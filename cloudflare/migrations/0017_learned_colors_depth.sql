-- learned_colors.depth_breakdown_json: applying ALTER via wrangler on very large remote D1 hits CPU 7429.
-- The Worker ensures this column at runtime (ensureLearnedColorsDepthColumn in cloudflare/src/index.ts).
SELECT 1;
