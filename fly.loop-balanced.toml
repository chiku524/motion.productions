# Balanced loop — UI-controlled exploit/explore, window extraction. Deploy from repository root:
#   fly apps create motion-loop-balanced   # first time only
#   fly deploy --config fly.loop-balanced.toml
app = "motion-loop-balanced"
primary_region = "iad"

kill_signal = "SIGTERM"
kill_timeout = 120

[build]
  dockerfile = "Dockerfile"

[deploy]
  wait_timeout = "10m"

[env]
  API_BASE = "https://motion.productions"
  LOOP_EXTRACTION_FOCUS = "window"
  LOOP_WORKER_OFFSET_SECONDS = "10"
  LOOP_WORKFLOW_TYPE = "main"
  HEALTH_PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  [http_service.concurrency]
    type = "requests"
    soft_limit = 2
    hard_limit = 4

  [[http_service.checks]]
    grace_period = "45s"
    interval = "60s"
    method = "GET"
    timeout = "5s"
    path = "/"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
