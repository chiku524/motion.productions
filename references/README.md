# Reference clips

Optional. A local MP4 you have **rights** to can be measured into the registries
(colors, sounds, motion windows) with `scripts/ingest_reference.py`.

That growth is **measurements only**. Loops do not start from, replay, or
composite frames from a reference clip.
